
[1000]

&a subsetEq &b ~~> ( forAll i in &a . freq(&a,i) <= freq(&b,i) )
                /\ ( forAll i in &b . freq(&a,i) <= freq(&b,i) )

    where &a hastype `mset of _`
    where &b hastype `mset of _`

$ [1000]
$ 
$ &a subsetEq &b ~~> forAll i : &dom . freq(&a,i) <= freq(&b,i)
$ 
$     where &a hastype `mset of _`
$     where &b hastype `mset of _`
$ 
$     where &a hasdomain `mset (..) of &dom`
