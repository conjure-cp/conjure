
preImage(&f,&x,&s)
    ~~>
(forAll i in defined(&f), &f(i) = &x . i in &s)
    /\
(forAll i in &s, &f(i) = &x . i in defined(&f))

    where &f hasType `function &fFrom --> &fTo`
    $ where &x hasType `&xType`
    $ where &s hasType `set of &sInner`
    $ where &xType = &fFrom
    $ where &fTo in &sInner
