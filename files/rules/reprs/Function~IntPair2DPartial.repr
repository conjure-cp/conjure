
~~> Function~IntPair2DPartial
~~> matrix indexed by [&a, &b] of (bool,&to)

    where &a hasType `int`
    where &b hasType `int`

*** function tuple (&a, &b) --> &to
~~> forAll i : &a . forAll j : &b . refn[i,j][1] = false -> dontCare(refn[i,j][2])
