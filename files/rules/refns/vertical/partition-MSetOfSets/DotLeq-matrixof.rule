[100]

&a <=lex &b ~~> flatten(refn(&a)) <=lex flatten(refn(&b))

    where &a hasType `matrix indexed by [_] of partition from _`
    where &b hasType `matrix indexed by [_] of partition from _`

    where &a hasRepr MSetOfSets
    where &b hasRepr MSetOfSets

