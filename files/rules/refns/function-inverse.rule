
[1000]

inverse(&f, &g)

    ~~>

(forAll (i,j) in toSet(&f) . (j,i) in toSet(&g))
    /\
(forAll (i,j) in toSet(&g) . (j,i) in toSet(&f))

    where &f hasType `function _ --> _`
    where &g hasType `function _ --> _`

