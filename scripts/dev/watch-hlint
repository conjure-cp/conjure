#/bin/sh

# had to 'set backupcopy=yes' in ~/.vimrc to make vim happy

watchmedo shell-command . \
    --patterns="*.hs;*.cabal" \
    --ignore-directories \
    --recursive \
    --command='scripts/dev/hlintThis "${watch_src_path}"' \

