
trigger:
- master

jobs:
- job: Conjure
  timeoutInMinutes: 0
  strategy:
    matrix:
      Linux:
        imageName: 'ubuntu-latest'
      Mac:
        imageName: 'macos-latest'
      Windows:
        imageName: 'vs2017-win2016'
  pool:
    vmImage: $(imageName)
  steps:
  - bash: PATH=${HOME}/.local/bin:${PATH} BUILD_TESTS=true make
    displayName: "Build Conjure"
  - bash: ls -l $(BIN_DIR)
    displayName: "ls BIN_DIR"
  - bash: PATH=${HOME}/.local/bin:${PATH} etc/build/install-minion.sh
    displayName: "Install Minion"
  - bash: PATH=${HOME}/.local/bin:${PATH} etc/build/install-glucose.sh
    displayName: "Install Glucose"
  - bash: PATH=${HOME}/.local/bin:${PATH} .stack-work/*/*/*/*/*/conjure-testing --limit-time 10 --hide-successes
    displayName: "Test"

