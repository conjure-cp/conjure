
alldifferent(m) ~~> forAll i,j : r, i < j . m[i] != m[j]

    where !(m hastype `matrix indexed by [_] of int`)
    where   m hastype `matrix indexed by [_] of _`
    letting r be domain indices(m,0)
