

Each test case is in an individual directory.


For each test case, create a directory, put:
 * An Essence problem spec
 * At least 1 parameter file
 * One solution file per parameter file


To run an individual test:
 * cd DIR
 * bash path/to/test_single.sh CONJURE_MODE


To run all tests:
 * bash path/to/test_all.sh full/path/to/fast-or-slow CONJURE_MODE1 CONJURE_MODE2 ...
 * by default, the above script will use all CPU cores.
   (`export PARALLEL="-j2"` to control the number of parallel executions.)


