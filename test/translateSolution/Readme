Run all the tests in `files/upTests` by running translateSolution on the the `.eprime.solution` files and comparing the resulting files with the expected results.

A test consists of  

<name>.essence
<name>/0001.eprime
<name>/0001.eprime.solution
<name>/0001.solution

and optionally 

<name>/<name>.param
<name>/<name>.essence-param
