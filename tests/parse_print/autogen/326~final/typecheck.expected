Error:
    In a 'such that' statement: together({[true; int(1)]}, partition({[true; int(1)]}) - partition({[false; int(1)]}))
    Error:
        When type checking: partition({[true; int(1)]}) - partition({[false; int(1)]})
        Arguments have unsupported types.
        lhs        : partition({[true; int(1)]})
        type of lhs: partition from matrix indexed by [int] of bool
        rhs        : partition({[false; int(1)]})
        type of rhs: partition from matrix indexed by [int] of bool