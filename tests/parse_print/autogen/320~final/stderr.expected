Error:
    In a 'such that' statement:
        (partition() : `partition from bool`) - partition({false}) in {partition({true, true, false, true, true})}
    Error:
        When type checking: (partition() : `partition from bool`) - partition({false})
        Arguments have unsupported types.
        lhs        : (partition() : `partition from bool`)
        type of lhs: partition from bool
        rhs        : partition({false})
        type of rhs: partition from bool