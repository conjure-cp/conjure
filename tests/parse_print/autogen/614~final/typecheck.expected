Error:
    In a 'such that' statement: apart({false}, partition({true}) - partition({false}))
    Error:
        When type checking: partition({true}) - partition({false})
        Arguments have unsupported types.
        lhs        : partition({true})
        type of lhs: partition from bool
        rhs        : partition({false})
        type of rhs: partition from bool