language Essence 1.3

find var1:
        partition (maxNumParts 1 - 0, minPartSize 3 , maxPartSize 3, regular) from
            relation of (int(4, 0))
find var2:
        relation (minSize 0 / 3,
                  irreflexive, symmetric, total, equivalence, partialOrder) of
            (set (maxSize 4) of bool * set (maxSize 4) of bool)
such that
    4 >= 5,
    !true,
    true = false
