language Essence 1.3

find var1:
        partition (numParts toInt(true)) from
            function (minSize 4 , maxSize 4, total, injective) bool --> int(3..5, 4)
find var2: bool
find var3:
        set (maxSize 4 - 2) of
            relation (minSize 2 , maxSize 3) of
                (int(4..4, 3..3) * int(4, 5) * bool * int(0, 5))
find var4: relation (size 1 / 5) of (matrix indexed by [int(5..5, 4)] of bool)
such that
    var2,
    var2
