language Essence 1.3

find var1: partition from bool
such that
    function(mset(true, true) --> var1) !=
    function(mset(true, false) --> partition({false})),
    false <-> var1 != var1,
    false,
    mset((mset() : `mset of int`), mset(-9, 0), mset(1, 1), mset(-10)) !=
    mset((mset() : `mset of int`), mset(0, -6), mset(7), mset(-10))
