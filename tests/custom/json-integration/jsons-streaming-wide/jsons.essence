$ jsonstream promises one solution per line. Solutions of this model are around 140
$ characters, well over the default --line-width of 120, so a solution that gets pretty
$ printed rather than streamed shows up as more lines than solutions, and each line
$ stops being valid JSON on its own.
letting e be new type of size 6

find x : function (total) e --> e
