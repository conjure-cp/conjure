Error:
    In a 'such that' statement: b = toInt(a)
    Error:
        When type checking: b = toInt(a)
        Cannot unify the types of the following.
        lhs        : b
        type of lhs: bool
        rhs        : toInt(a)
        type of rhs: int
