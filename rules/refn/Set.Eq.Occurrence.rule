
a = b ~~> (forAll i in toSet(ind_a) intersect toSet(ind_b) . m_a[i] = m_b[i])
       /\ (forAll i in toSet(ind_a) - toSet(ind_b) . m_a[i] = 0)
       /\ (forAll i in toSet(ind_b) - toSet(ind_a) . m_b[i] = 0)

    where
        a hastype `set of _`,
        b hastype `set of _`,
        repr(a,Occurrence),
        repr(b,Occurrence)

    letting m_a be refn(a)
    letting ind_a be indices(m_a,0)

    letting m_b be refn(b)
    letting ind_b be indices(m_b,0)
